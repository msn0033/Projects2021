@inject ERPv1.Data.ApplicationDbContext _dbcontex
@model dynamic
@{ 
    var Acctype = _dbcontex.AccountChartCounter.Select(x => new SelectListItem
    {
        Value = x.Id.ToString(),
        Text = x.AccountTypeAr
    }
    ).OrderBy(x => x.Text).ToList();

}
@{ 
    var htmlAtt = ViewData["htmlAtt"] ?? new { };

}
@Html.DropDownList("", Acctype, "--اختار--", htmlAtt)

